specVersion: "v1beta"
serviceId: "transport_service_app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "transport_service_app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
